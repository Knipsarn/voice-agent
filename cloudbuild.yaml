steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -f
      - Dockerfile
      - -t
      - gcr.io/ldk-clean/voice-bridge-service
      - .

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - push
      - gcr.io/ldk-clean/voice-bridge-service

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - voice-bridge-service
      - --image=gcr.io/ldk-clean/voice-bridge-service:latest
      - --region=europe-west1
      - --project=ldk-clean
      - --update-secrets=TELNYX_API_KEY=TELNYX_API_KEY:latest,POST_PROCESSOR_SECRET=POST_PROCESSOR_SECRET:latest
      - --set-env-vars=POST_PROCESSOR_URL=https://post-processor-service-360579353014.europe-west1.run.app,TELEPHONY_URL=https://telephony-service-360579353014.europe-west1.run.app
      - --quiet

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - services
      - update-traffic
      - voice-bridge-service
      - --to-latest
      - --region=europe-west1
      - --project=ldk-clean

images:
  - gcr.io/ldk-clean/voice-bridge-service

options:
  logging: CLOUD_LOGGING_ONLY
